name: Public tree gate

on:
  push:
  pull_request:

jobs:
  public-tree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate publishable index
        shell: pwsh
        run: ./tools/check-public-tree.ps1
